# TODO

## Pending Features

- [ ] **Project-level filtering in dashboard UI** — Add a project filter/selector to the dashboard so users can view usage scores, frequency, trends, and coverage scoped to a specific project. Requires `project_dir` to be recorded in the DB schema first (part of plugin packaging work #ft-4).
